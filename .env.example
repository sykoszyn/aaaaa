# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role — SOLO server-side. Nunca exponer con el prefijo NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Secreto compartido para autorizar el cron de Vercel (ver vercel.json).
# Generalo con: openssl rand -hex 32
CRON_SECRET=
